# ============================================
# Environment Variables for Microservices
# ============================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!

# --------------------------------------------
# GitHub Configuration (REQUIRED)
# --------------------------------------------
# GitHub Personal Access Token with repo and webhook permissions
# Get from: https://github.com/settings/tokens
GITHUB_TOKEN=your-github-token-here

# GitHub Webhook Secret for signature validation
# Should match the secret configured in your GitHub webhook settings
GITHUB_WEBHOOK_SECRET=your-webhook-secret-here

# --------------------------------------------
# AI Provider Configuration
# --------------------------------------------
# AI Provider: "ollama" or "gemini"
AI_PROVIDER=ollama

# Ollama Configuration (if AI_PROVIDER=ollama)
OLLAMA_MODEL=qwen2.5-coder:7b
# Available models: qwen2.5-coder:3b, qwen2.5-coder:7b, qwen2.5-coder:14b

# Google Cloud AI / Gemini Configuration (if AI_PROVIDER=gemini)
# Only required if using Gemini AI
GCP_PROJECT_ID=
GCP_LOCATION=us-central1
# Note: Requires Google Cloud credentials configured separately

# --------------------------------------------
# Observability (Optional Overrides)
# --------------------------------------------
# These are already set in docker-compose.yml, override if needed

# OpenTelemetry Collector Endpoint
OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318

# Jaeger UI
# Access at: http://localhost:16686

# Prometheus
# Access at: http://localhost:9090

# Grafana
# Access at: http://localhost:3000
# Default credentials: admin / admin

# --------------------------------------------
# Kafka Configuration (Optional)
# --------------------------------------------
# Kafka is running inside Docker network
# External access: localhost:9092
KAFKA_BOOTSTRAP_SERVERS=kafka:9092

# --------------------------------------------
# Service Ports (Informational)
# --------------------------------------------
# These ports are mapped in docker-compose.yml
# webhook-service:     8080
# context-service:     8082
# review-service:      8083
# integration-service: 8084

# --------------------------------------------
# Usage Instructions
# --------------------------------------------
# 1. Copy this file: cp .env.example .env
# 2. Fill in your GITHUB_TOKEN and GITHUB_WEBHOOK_SECRET
# 3. (Optional) Adjust AI_PROVIDER and related settings
# 4. Run: docker-compose up -d
