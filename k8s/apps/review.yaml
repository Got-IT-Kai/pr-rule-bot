# k8s-review.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: review-service
  namespace: pr-bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: review-service
  template:
    metadata:
      labels:
        app: review-service
    spec:
      containers:
        - name: review-service
          image: asia-northeast3-docker.pkg.dev/pr-rule-bot/pr-bot/review-service:latest
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: pr-bot-config
            - secretRef:
                name: pr-bot-secrets
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8083
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 5
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: review-service
  namespace: pr-bot
spec:
  type: ClusterIP
  selector:
    app: review-service
  ports:
    - port: 8083
      targetPort: 8083

