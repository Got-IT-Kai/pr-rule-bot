spring:
  application:
    name: webhook-service

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.code.webhook: DEBUG
    org.springframework.web: INFO
